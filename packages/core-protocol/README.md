# core-protocol

Owns command/response compatibility with upstream Ubertooth behavior.

Near-term tasks:
- map read-only command semantics
- define typed error categories
- document parity gaps per command
